/**
 * 7.1 定义抽象数据类型
 * @Author Bob
 * @Eamil 0haizhu0@gmail.com
 * @Date 2017/8/15
 */
int main(){
  /**
   * 数据抽象依赖于接口和实现分离的编程技术。
   *
   * 在第一章中的Sales_item类是一个抽象数据类型，我们通过它的接口来使用Sales_item对象。我们不能访问它的数据成员。事实上，
   * 我们甚至不知道这个类有哪些数据成员。
   *
   * 与之相反，Sales_data类不是一个抽象数据类型。它允许类的用户直接访问它的数据成员，并且要求由用户来编写操作。要想把Sales_data
   * 变成抽象数据类型，我们需要定义一些操作以供类的用户使用。一旦Sales_data定义了它自己的操作，我们就可以封装(隐藏)它的数据成员了。
   */
  return 0;
}
