/**
 * 8.3 string 流
 * @Author Bob
 * @Eamil 0haizhu0@gmail.com
 * @Date 2017/9/6
 */
int main(){
  /**
   * sstream定义了三个类型来支持内存IO，可以从string读取数据，也可以向string写入数据，就像string是一个IO流一样。
   *
   * istringstream  从string读取数据
   * ostringstream  向string写入数据
   * stringstream   既可以读取又可以写入数据
   *
   */
  return 0;
}
