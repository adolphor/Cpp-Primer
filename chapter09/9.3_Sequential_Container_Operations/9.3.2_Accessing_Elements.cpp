/**
 * 9.3.2 访问元素
 * @Author Bob
 * @Eamil 0haizhu0@gmail.com
 * @Date 2017/9/19
 */
int main(){
  /**
   * 包括array在内的每个顺序容器都有一个front元素，除了forward_list之外的所有顺序容器都有一个back成员函数。
   * 这两个操作分别返回首元素和尾元素的引用。
   */

  /**
   * 访问成员函数返回的是引用，可以用来改变容器中元素的值。
   */

  /**
   * 下标操作和安全的随机访问：直接使用下标可能出现运行时异常，使用at成员函数可能抛出out_of_range异常。
   */
  return 0;
}
